[pytest]
pythonpath = . src ../common-lib
addopts = -m "not integration"
markers =
    integration: live integration tests requiring external services or network connectivity
    unit: isolated unit tests